ADD_LIBRARY(ThreadPool STATIC ThreadPool.cc)

TARGET_SOURCES(ThreadPool PUBLIC
  ThreadPool.hpp
  Queue.hpp
  Queue-inl.hpp
)

TARGET_INCLUDE_DIRECTORIES(ThreadPool PUBLIC
  $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}>
)

TARGET_LINK_LIBRARIES(ThreadPool PUBLIC
  Utils
  Sync
)
